{
  "name": "lexi-ai-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "wrangler pages dev ./dist --ai AI",
    "deploy": "npm run build && wrangler pages deploy ./dist"
  },
  "dependencies": {
    "hono": "^4.3.11",
    "eventsource-parser": "^1.1.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "@hono/vite-cloudflare-pages": "^0.3.0",
    "@hono/vite-dev-server": "^0.12.1",
    "typescript": "^5.4.5",
    "vite": "^5.2.12",
    "wrangler": "^3.57.2"
  }
}
